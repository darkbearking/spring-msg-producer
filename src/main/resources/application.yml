server:
  port: 8080
spring:
  application:
    name: spring-msg-producer
#  下面的配置即默認配置，可以不必寫
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
eureka:
  client: 
    serviceUrl:
      defaultZone: http://localhost:8761/eureka